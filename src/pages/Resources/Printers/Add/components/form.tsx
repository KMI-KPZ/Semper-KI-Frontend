import React from "react";
import { useTranslation } from "react-i18next";

interface ResourcesPrintersAddFormProps {}

const ResourcesPrintersAddForm: React.FC<ResourcesPrintersAddFormProps> = (
  props
) => {
  const {} = props;
  const { t } = useTranslation();

  return <div className="">ResourcesPrintersAddForm</div>;
};

export default ResourcesPrintersAddForm;
